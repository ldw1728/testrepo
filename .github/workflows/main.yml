name: testAction

on:
  push:
    branches: [ master ]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: checkout
        uses: actions/checkout@v3
      - name: check
        run: |
          pwd
          ls -al
      - name: deploy
        uses: wlixcc/SFTP-Deploy-Action@v1.2.4
        with:
          username: ec2-user
          server: 13.125.250.63
          port: 22
          ssh_private_key: ${{ secrets.FTP_SSH_KEY }}
          local_path: ./*
          remote_path: /prod/app/publishing/test/
          sftp_only: true

          
          

          
          
           
        

           
        
